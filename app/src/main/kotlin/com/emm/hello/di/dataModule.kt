package com.emm.hello.di

import org.koin.dsl.module

/**
 * Módulo de datos — SQLDelight y Room se configuran en newModule.
 * Este archivo se mantiene como placeholder para futuros data sources.
 */
val dataModule = module {
    // Room (AppDatabase) fue reemplazado por SQLDelight en newModule.
    // No quedan dependencias activas de Room.
}
