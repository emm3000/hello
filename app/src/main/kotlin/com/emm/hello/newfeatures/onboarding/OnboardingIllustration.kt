package com.emm.hello.newfeatures.onboarding

/**
 * Placeholder vector illustration keys for each onboarding page.
 * Actual drawables can be swapped in later without changing the enum contract.
 */
enum class OnboardingIllustration {
    Decks,
    SpacedRepetition,
    Grading,
}
